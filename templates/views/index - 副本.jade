extends ../layouts/default

block content
	.container: .jumbotron
		img(src='/images/13.png', width='200')
		h1 Welcome
		p This is <strong>Advanced Database Technology  Report & Marking System </strong>
		hr
		if user && user.canAccessKeystone
			p
				a(href='/keystone/signin').btn.btn-lg.btn-primary Open the Admin UI
		else
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.
		hr
		p Remember to 
